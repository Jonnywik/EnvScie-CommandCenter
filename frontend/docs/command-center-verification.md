# Command Center Verification Notes

The updated dashboard was verified on `http://localhost:3001` in demo mode. The overview rendered the operating period, four incident objectives, task board, responder posture, situation map, SOS queue, verified alert feed, source health, and evacuation capacity. The Live SOS subtab rendered search, status/channel filters, triage rows with location confidence and channel age, a pending-acknowledgement count, dispatch guardrails, SMS fallback guidance, and a shift-handover action. The API returned an operations snapshot with 3 teams, 6 resource lines, 3 hazard constraints, and 4 operational tasks.

Verified Alerts rendered two healthy source cards, severity and hazard filters, a feed-review action, and a warning register with verification and public-bulletin actions. Risk Map rendered togglable Flood/SOS/Centers/Route layers, three hazard constraints, a route-safety guardrail, and a safe-route preview control backed by `/v1/routes/safest-center`.

Evacuation Centers rendered three center capacity cards with intake status, amenities, distance estimates, capacity verification, and resupply actions, plus a barangay readiness table with population at risk, movement state, assigned shelter/transport, last contact, and unmet needs. Resources rendered category filtering, six accountable resource lines, available-versus-total counts, location/owner, operational state, check/reserve/resupply actions, and a communications tracker with acknowledgement coverage and queued/released bulletin controls.

Validation completed on 12 August 2026: `NODE_ENV=production pnpm exec tsc --noEmit` and `NODE_ENV=production pnpm run build` passed; the backend suite passed all 17 tests with explicit demo PostgreSQL settings; the demo backend returned healthy status and an operations snapshot; and an authenticated dispatcher action was recorded and confirmed in the audit stream. The development services are running on `localhost:3001` and `localhost:8000`.

The localhost processes were terminated and the services were relaunched inside the Manus sandbox. The public preview was verified at `https://3001-i945ssem2o6y5z2f9ewj6-9f6b4420.sg1.manus.computer/`, with the API proxied through `https://8000-i945ssem2o6y5z2f9ewj6-9f6b4420.sg1.manus.computer/v1`. The browser rendered the Command Center title, overview metrics, incident objectives, task board, responder posture, risk map, SOS queue, verified alerts, and evacuation capacity through the Manus preview.

GIS integration smoke-test: `GET /v1/gis/map` returned a demo-seed spatial snapshot with five tracked assets, three hazard layers, SOS positions, evacuation centers, and a geographic bounding box. `POST /v1/gis/routes/optimize` returned a safe route with distance, estimated travel time, avoided-hazard count, blocked-segment count, route freshness, and a dispatch warning. The Overview browser view rendered the operational GIS map, layer toggles, five live resource markers, hazard polygons, center/SOS markers, source freshness, and the position-confidence guardrail. The Evacuation Centers tab also rendered successfully during tab navigation.

Risk Map browser verification: the GIS view rendered the five layer toggles, three hazard constraints, five tracked resources, SOS and center markers, live snapshot freshness, and the routing guardrail. Clicking `Optimize nearest safe route` returned Municipal Gymnasium at 416 m with an approximately 13-minute travel estimate, three hazards avoided, one blocked segment, a `SAFE` status, route freshness, and the demo-road-graph warning. The action produced the expected `Recorded: route · previewed` audit toast.

Live tracking browser verification: selecting Alpha Rescue · Truck 01 opened the GIS resource inspector with assignment, en-route state, four-minute position age, ±12 m accuracy, and 82% battery. Clicking `Mark position checked` updated the marker freshness to just now and produced the expected `Recorded: gis · resource_position_checked` audit toast.

Response Groups browser verification: the new tab rendered five tracked emergency groups with availability, agency, group type, personnel ready/total, readiness score, specialties, current location, location source/freshness, vehicle or asset, call sign, communications channel, current assignment, response estimate, equipment, constraints, and last check-in. Search, availability, and specialty filters were present, and the selected-group panel showed Alpha Rescue’s readiness, GPS freshness, ±12 m accuracy, specialties, equipment, constraints, assignment, and response estimate. The assignment desk exposed target-type and target selectors, assignment notes, and the guarded assignment action; the roster endpoint and authenticated dispatcher assignment smoke test both returned successfully.

Manus preview verification: the public dashboard initially remained on its loading state because the browser bundle was using the localhost API fallback and the proxied API origin was not allowed by CORS. The preview was repaired by restarting the frontend with the Manus API URL and restarting FastAPI with the public dashboard origin explicitly allowed. The browser then rendered the full Command Center at `https://3001-i945ssem2o6y5z2f9ewj6-9f6b4420.sg1.manus.computer/`. The Response Groups tab showed five tracked groups, two available groups, two assigned/deployed groups, one limited group, zero stale positions, live location freshness, GPS accuracy, readiness, specialties, equipment, constraints, and the assignment desk. The Overview simultaneously rendered the live GIS map with five resource markers, hazard polygons, SOS markers, evacuation centers, layer toggles, and the demo GIS snapshot freshness badge.

Communications browser verification: the Communications tab rendered four logged messages, three simulated audio dispatch clips with waveform controls and playing/played status, channel-health indicators, direction/channel/priority filters, an outbound command composer, linked-incident selection, and the simulation boundary warning. The tab exposed coordination metrics, a live communication log, the simulated audio-dispatch feed, and the recommendation workspace.

Dispatch recommendation verification: selecting the critical TRAPPED incident for Barangay 1 generated `dispatch-score-v1` recommendations and recorded the `dispatch.recommendations_generated` audit action. Specialty normalization now matches roster labels consistently across spaces, underscores, and hyphens. The top result was Delta Water Rescue with a score of 96, `recommended` eligibility, boat-operations and swiftwater-rescue matches, 1,391 m estimated distance, a 10-minute response estimate, 5-minute position freshness, 91/100 readiness, and a missing household-extraction constraint. Alpha Rescue retained its full specialty match but was ranked `constrained` because of active assignment/readiness constraints. The recommendation panel exposed score factors, reasons, constraints, advisory-engine metadata, and the assignment handoff control.

Manus runtime reinitialization verification: stale listeners were replaced with a demo-mode FastAPI process on `0.0.0.0:8000` and a Next.js development process on `0.0.0.0:3001`, configured against the public API proxy. `POST /v1/auth/demo-login`, `GET /v1/health`, `GET /v1/sync/bootstrap`, `GET /v1/dashboard/summary`, `GET /v1/dashboard/operations`, `GET /v1/response-groups`, and `GET /v1/gis/map` returned successfully. The public preview at `https://3001-i945ssem2o6y5z2f9ewj6-9f6b4420.sg1.manus.computer/` reached the branded operational Overview with metrics, objectives, task board, responder posture, GIS map, SOS queue, verified alerts, and evacuation capacity.
